spring:
  datasource:
    url: jdbc:derby:/home/hashiwa/java/springboot/db_test;create=true
    username: test
    password: test
    driverClassName: org.apache.derby.jdbc.EmbeddedDriver
  jpa:
    hibernate:
      ddl-auto: update
